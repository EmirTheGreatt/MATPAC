#include "matrix.h"

void  gram_schmidt(Matrix *M)
{
	Matrix *proj = zeros(1, M -> cols);
	for (size_t col1 = 0; col1 < M -> cols; col1++)
	{
		for (size_t col2 = 0; col2 < col1; col2++)
		{
			
		}	
	}
}
